# 靈明输入法构词程序

---

使用方法：将灵明的拆分与词库文件放在程序运行目录下，运行程序即可：

```bash
$ ls .     
Cargo.lock
Cargo.toml
README.md
src/
target/
yuling_chaifen.dict.yaml
yuling_chaifen_tw.dict.yaml
yuling.full.dict.yaml
yuling_sc.words.dict.yaml
yuling_tc.words.dict.yaml

$ cargo run
```
